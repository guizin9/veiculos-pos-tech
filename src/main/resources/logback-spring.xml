<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

    <!-- JSON estruturado — Cloud Logging (profile gcp) -->
    <springProfile name="gcp">
        <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder class="net.logstash.logback.encoder.LogstashEncoder">
                <includeMdcKeyName>correlationId</includeMdcKeyName>
                <includeMdcKeyName>sagaId</includeMdcKeyName>
                <customFields>{"service":"veiculos-api"}</customFields>
                <fieldNames>
                    <timestamp>timestamp</timestamp>
                    <version>[ignore]</version>
                    <levelValue>[ignore]</levelValue>
                    <level>severity</level>
                </fieldNames>
            </encoder>
        </appender>
    </springProfile>

    <!-- Texto legível — dev local, docker e testes -->
    <springProfile name="!gcp">
        <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder>
                <pattern>%d{yyyy-MM-dd'T'HH:mm:ss.SSSXXX} [%X{correlationId:-}] [%X{sagaId:-}] %5p ${PID:-} --- [%15.15t] %-40.40logger{39} : %m%n</pattern>
            </encoder>
        </appender>
    </springProfile>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>

    <logger name="org.hibernate.SQL" level="WARN"/>
    <logger name="org.hibernate.orm.jdbc.bind" level="WARN"/>
</configuration>
